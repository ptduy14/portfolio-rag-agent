name: Keep Systems Alive
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up systems
        run: |
          curl -s https://ptduy14-portfolio-rag-agent.hf.space/heartbeat
